<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="noAnimTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="cardBreed">
        <item name="android:textSize">@dimen/cardBreedText</item>
        <item name="android:fontFamily">casual</item>
        <item name="android:background">@color/colorWhiteTransparent</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingRight">@dimen/cardBreedTextPaddin</item>
        <item name="android:paddingLeft">@dimen/cardBreedTextPaddin</item>
    </style>

    <style name="search">
        <item name="android:layout_margin">@dimen/searchMargin</item>
        <item name="android:background">@color/colorWhiteTransparent</item>
    </style>

</resources>
